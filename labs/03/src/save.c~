#include <stdio.h>
#include <stdlib.h>


int main(int argc, char * argv[]){
  
  int c;
  FILE * output;

  //TODO: Complete the program
  if((output = fopen("saved.out", "w")) == NULL){
    fprintf(stderr,"ERROR: fopen: file doesn't exist?\n");
    exit(1); 
  }

  while(fscanf(output, $1) == 1){

  }
  fclose(output);
  return 0;
}
